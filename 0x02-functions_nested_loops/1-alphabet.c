#include "main.h"
#include <stdio.h>

/**
 * main - entry point
 * Description: prints the alphabet, in lowercase\n
 * Return: is 0.
 */
int main(void)
{
	for (int x = 'a'; x <= 'z'; x++)
	{
		putchar(x);
	}
	return (0);
}
